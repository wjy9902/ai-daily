# 日报摘要恢复：实施与验收记录

本记录覆盖实现与独立试运行验收；线上部署状态以合并记录和部署记录为准。试运行未发布刊期或上传公众号。

## 实施范围

- 去除摘要和标题的硬截断；已校验的 TL;DR 完整保留。
- 正常编辑成功时不新增调用。降级最多处理前 12 条、每批 4 条、最多 3 批，补抓及生成共用 180 秒期限，计入现有正文预算。
- 原摘要明确完整且长度合适时直接使用；其余先利用已有证据生成完整摘要。证据不足复用正文补抓，每个事件本轮最多尝试一次。失败才跳过；全部失败为 L3。
- 逐条核验原文引用、长度和明显残缺；合格结果不因同批其他条目失败而丢失。引用来源优先进入展示来源列表。
- 新刊和手动替换均经过发布前检查；历史读取和重建不重新审判旧文案。未调整出刊等级、定时器、模型配置或主备策略。
- 真实 HTTP 请求统计包括失败请求；内部记录状态、失败轮次、脱敏错误类别和校验类别。原始响应、密钥和提示词不进入日志；公开错误不回显模型校验内容。

## 验证

- 隔离 worktree 基线：555 项测试通过。提交前复核完整测试：591 项通过；lint、mypy、pyright、构建和 diff 检查均通过。
- 新增测试覆盖真实 8 条截断样本、批次部分失败、补抓、预算耗尽、凭证缺失、超时、采集器生命周期、发布拒绝、历史重建，以及首次响应后第二次请求报 400。
- 同时覆盖小数、英文缩写、完整网址、Markdown 链接、无句号结尾和英寸符号，避免将普通内容误判为残缺。
- 运行 `uv run ruff check .`、`uv run mypy src`、`uv run pyright`、`uv run pytest --disable-warnings`、`uv build` 和 `git diff --check`。

## 真实模型试运行

使用生产首轮 `2026-09-23-e2c5d642` 的历史候选，在独立临时目录运行新代码；没有调用出刊入口，也没有改动线上代码、预算台账、刊期或公众号。

单次试运行限制为 180 秒、20 次请求、6 元总预算，并启用请求前成本预留；实际还受正文阶段预算份额约束。

- 首轮：8 条中恢复 8 条；2 次请求，按配置单价估算 0.13294049 元。
- 人工核对：关键数字均能在原始证据中找到，正文不再以半截词句或网址结束。
- 审读发现一条厂商性能宣传缺少明确归属，因此补强提示词，要求宣传性性能、降本数字注明厂商自述，未来计划保留计划或预计。
- 对该条进行一次针对性复测：恢复 1 条；1 次请求，估算 0.03661787 元。复测稿明确来源，并准确概括官方首用建议。
- 两轮合计约 0.16956 元。以上为配置价格估算，不是服务商结算账单。

## 前后对照

以下正文采用首轮生成结果；Opus 首用建议采用提示词修正后的复测结果。

### 腾讯混元发布Hy Image3.5 preview，API 2K图0.15元/张，能力较上代提升约30%

原稿末尾：`支持文生图和图生图，单次最多可上传 5 张图作为参考，并支持多种尺寸比`

恢复后：腾讯混元发布图像模型Hy Image3.5 preview，支持文生图、图生图与多轮对话，单次最多上传5张参考图，最高2K输出。内部数百名专业设计师GSB盲测显示较Hy Image3.0提升约30%，与Seedream 5.0 pro持平，略优于Nano-Banana Pro和Qwen-Image-3.0 Pro。腾讯云API 2K图像定价0.15元/张，仅对输出图片收费；模型已上线元宝、WorkRally、OnSolo、Miora、WorkBuddy、ima等应用，元宝灰度期间生图请求量提升超50%。

### Claude Developers给出Opus 5.5首用建议：交办完整任务并明确“完成”标准

原稿末尾：`Our playbook: https://claude.dev/bl`

恢复后：Claude Developers在X平台发文称，首次使用Claude Opus 5.5时，可交办完整任务并定义“完成”标准与检查时机；无需输入“think carefully”，因为模型总会先思考；长时间运行后应确认其继续所需。官方并附操作手册链接。

### 阿里平头哥发布AI芯片真武V900，算力提升至M890的3倍，单集群可扩展至50万卡

原稿末尾：`头哥已对数据中心芯片进行全面布局，包括真武系列 GPU、倚天系列 CP`

恢复后：2026云栖大会上，阿里平头哥发布新一代AI芯片真武V900，算力提升至真武M890的3倍，基于其构建的单一集群可扩展至50万卡，可支撑前沿模型训练和推理。该芯片为训推一体AI芯片，支持216GB显存、1200GB/s片间互联、FP8/FP4精度；搭载真武V900的全新磐久超节点服务器将于2027年Q1上市，下一代真武J900计划2028年Q3上市。

### 千问披露Qwen3.8-Max芯片设计实验：60小时完成NoC模块RTL到物理实现，面积降42%

原稿末尾：`基础设施，也能依据真实工具反馈参与设计、验证和优化。为此，Qwen 分`

恢复后：千问团队披露两项生产级实验：Qwen3.8-Max仅凭一份NoC模块规格，在工业级EDA工具链中无逐轮人工干预持续运行60小时以上，完成RTL、验证到物理实现，标准单元数下降29%、物理面积下降42%、功耗估算下降59.5%；另一实验中，Qwen3.8-Max用56小时将Qwen3.8-Flash-Next适配并优化到平头哥真武M890，长文本首token延迟下降47%、每输出token延迟下降60%，日常对话单实例吞吐提升96%。

### 小米正式发布并开源 MiMo-V2.6 系列模型

原稿末尾：`报道过，小米把一场持续近六天、合计花费约 347 万美元的强化学习训练`

恢复后：小米正式发布并开源 MiMo-V2.6 系列，含 Pro、Flash 及 Pro-UltraSpeed。Pro 在 Artificial Analysis Intelligence Index v4.3 得 46.32 分，超过 Kimi K3 与 Qwen3.8 Max，官方称其为当前该榜单最高开源模型；UltraSpeed 输出速度最高为 Pro 的 20 倍。强化学习训练近六天、合计约 347 万美元，Flash 与 Pro 分别完成 30 个 RL step，成本约 85 万、262 万美元。Flash API 输入/输出为每百万 token 1 元/2 元，Pro 为 3 元/6 元。

### OpenAI与Anthropic发布新模型，主打更低成本

原稿末尾：`r complex knowledge work. And OpenA`

恢复后：Anthropic发布主力模型Opus 5.5，面向编程等复杂知识工作；OpenAI发布GPT-6 Sol和Luna，定位中端或更小、注重效率与速度。两家均承诺以更低价格提供更多能力。

### OpenAI GPT-6 Sol/Luna与Anthropic Opus 5.5同日发布，成本大降

原稿末尾：`novations to make models cheaper wh`

恢复后：Axios报道，OpenAI周二发布GPT-6 Sol和GPT-6 Luna，向Axios表示顶级企业客户成本较同系列前代降低50%；Anthropic同日发布Opus 5.5，称运行成本比Opus 5低约40%且保持顶级智能。xAI周一发布Grok 4.7主打性价比，但24小时内价格优势被大量抵消。DeepSeek本月发布的V4.1 Flash登上OpenRouter榜首，本周使用量激增172%。

### 千问办公发布企业上下文等产品，称业内首个企业级Agent

原稿末尾：`进入公司的工作流。 企业真正需要的，是一个能够理解业务上下文、知道该和`

恢复后：千问办公在杭州云栖大会发布企业上下文（Enterprise Context）、应用、QwenNote、安全中心等。阿里巴巴集团副总裁、千问办公CEO陈宇森称其为业内首个企业级Agent产品，需懂业务、能协同、信得过。Enterprise Context可把群聊、文档、知识库等整理成结构化企业上下文，已向古茗、富立卡、长安汽车等开放邀请测试；QwenNote A2支持语音转写，仅保留文字和纪要，不保留原始音频。

## 剩余限制与后续

- HTTP 400 的具体服务端原因仍未定案，本次补足诊断，不宣称修复未知的服务商问题。
- 轻量规则与引用匹配不能保证所有语义正确；引用真实也不等于全部结论成立。不增加全篇审稿或复杂评分系统。
- 单条返回无效结果时保留其他合格条目，不再增加额外修复循环。预算、时限或凭证问题导致恢复无法继续时，明确记录原因。
- 公众号制作及上传前核对最新刊期指纹；版本变化则重新导入审稿。本轮不改造公众号系统。
- 提交与部署已由用户明确授权；上线后再观察连续 3 个出刊日，关注恢复数量、失败原因和额外成本。尚未部署，因此不能报告线上观察结果。
